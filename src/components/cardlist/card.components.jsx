import React from 'react';
import 'tachyons';
const Card = ({ topic }) => {
    return (
            <div>
                <h2 className="white">{topic}</h2>
            </div>

    );
}

export default Card;
